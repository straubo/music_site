import { RainContainerDirective } from './rain-container.directive';

describe('RainContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new RainContainerDirective();
    expect(directive).toBeTruthy();
  });
});
